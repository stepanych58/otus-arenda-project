{{- if .Values.ingress2.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "user-service-chart.fullname" . }}-2
  labels:
    {{- include "user-service-chart.labels" . | nindent 4 }}
  {{- with .Values.ingress2.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  namespace: {{ .Values.projectNamespace }}
spec:
  {{- with .Values.ingress2.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingress2.tls }}
  tls:
    {{- range .Values.ingress2.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress2.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "user-service-chart.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          {{- end }}
    {{- end }}
  {{- end }}
