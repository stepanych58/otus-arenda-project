apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-service-chart.fullname" . }}
  labels:
    {{- include "java-service-chart.labels" . | nindent 4 }}
  namespace: {{ .Values.projectNamespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "java-service-chart.selectorLabels" . | nindent 4 }}
