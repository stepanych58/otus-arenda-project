replicaCount: 1
image:
  repository: bitnamilegacy/kafka

auth:
  clientProtocol: PLAINTEXT
  interBrokerProtocol: PLAINTEXT

listeners:
  client:
    protocol: PLAINTEXT
  controller:
    protocol: PLAINTEXT
  interbroker:
    protocol: PLAINTEXT
  external:
    protocol: PLAINTEXT

persistence:
  enabled: false
